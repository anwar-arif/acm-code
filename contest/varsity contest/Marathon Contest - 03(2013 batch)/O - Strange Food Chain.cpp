#include <bits/stdc++.h>
using namespace std ;
#define pf printf
#define sc scanf
#define pb push_back

int main()
{
        int ;
        sc("%d",&tst);
        while(tst--)
        {
                sc("%d%d",&n,&k);
                for(i = 1; i <= k; i++)
                {
                        sc("%d%d%d",&p,&x,&y);

                        if(p == 1)
                        {

                        }

                        else if(p == 2)
                        {

                        }
                }
        }
}
